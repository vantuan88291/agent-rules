# AGENTS.md - Workspace Config

## Rules Loading

Core rules in `SOUL.md` always apply.

This workspace is **single-purpose** (homestay Google Sheets).

Always load and follow the `homestay-sheets` skill for any Sheets task in this workspace (especially monthly reports/"thống kê tháng").

Rules + bot identity live in `SOUL.md`. Reference docs:
- `SHEET_SCHEMA.md` (layout + labels)
- `APPSCRIPT_MONTH_ROLLOVER.md` (duplicate month tab via Apps Script)

